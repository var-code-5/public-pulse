export enum Role {
  CITIZEN = 'CITIZEN',
  GOVERNMENT = 'GOVERNMENT',
  ADMIN = 'ADMIN'
}

export enum IssueStatus {
  PENDING = 'PENDING',
  ONGOING = 'ONGOING',
  PAUSED = 'PAUSED',
  CLOSED = 'CLOSED'
}

export enum VoteType {
  UPVOTE = 'UPVOTE',
  DOWNVOTE = 'DOWNVOTE'
}
